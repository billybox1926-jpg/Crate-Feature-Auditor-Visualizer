[package]
name = "helper"
version = "0.1.0"
edition = "2021"

[features]
default = []
build-only = []

[dependencies]
leaf = { path = "../leaf", default-features = false, features = ["build-transitive"] }
